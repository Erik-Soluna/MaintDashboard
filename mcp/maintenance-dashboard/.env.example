# Copy to .env (gitignored) or set in your MCP client config. NEVER commit the token.
MAINT_API_BASE=https://maintenance.errorlog.app
MAINT_API_TOKEN=replace-with-token-from-create_api_token
# MAINT_API_VERIFY_TLS=0   # only for self-signed dev hosts

# Cleanup Plan for Latest Branch

## Files to Remove

### 1. Old Docker Entrypoint Scripts (keep only docker-entrypoint.sh)
**Keep:**
- `docker-entrypoint.sh` (currently in use)

**Remove (unused):**
- `docker-entrypoint-fixed.sh`
- `docker-entrypoint-debug.sh`
- `docker-entrypoint-corrected.sh`
- `docker-entrypoint-old.sh`
- `docker-entrypoint-legacy.sh` (referenced in docs but not actively used)
- `docker-entrypoint-clean.sh`

**Keep but verify:**
- `docker-entrypoint-automated.sh` (used in portainer-stack-dev-automated.yml)

### 2. Root-Level Test/Fix/Debug Scripts
**Move to appropriate directories or remove:**

Root level files that should be in `tests/` or `scripts/`:
- `test_*.py` files in root → Move to `tests/` or remove if duplicates
- `fix_*.py` files in root → Move to `scripts/` or remove
- `check_*.py` files in root → Move to `scripts/` or remove
- `create_*.py` files in root → Move to `scripts/` or remove
- `debug_*.py` files in root → Move to `debug/` or remove
- `force_*.py` files in root → Remove (temporary fixes)
- `clear_*.py` files in root → Move to `scripts/` or remove
- `apply_remaining_migrations.py` → Move to `scripts/` or remove
- `populate_activity_types.py` → Move to `scripts/` or remove

### 3. Old Form Response HTML Files
**Remove:**
- `form_response.html` (generated by debug scripts)
- `minimal_form_response.html` (generated by test scripts)
- `admin_form_response_*.html` files (test artifacts)

### 4. Builder.io / Vibes-r-us Directory
**Remove:**
- `vibes-r-us/` directory (entire directory - Builder.io code)

### 5. Miscellaneous Files
**Remove:**
- `cookies.txt` (test file)
- `tatus` (typo file)
- `test_equipment_import.csv` (move to tests/ if needed)
- `test_equipment_import_edge_cases.csv` (move to tests/ if needed)
- `test_login.json` (test artifact)
- `test_migration.json` (test artifact)

### 6. Old SQL Files
**Review and potentially remove:**
- `add_timezone_column.sql` (if migrations handle this)

### 7. Deprecated Directory
**Keep but review:**
- `deprecated/playwright-2025-10-09/` (archived, can stay)

## Files to Keep
- All files in `scripts/` (organized)
- All files in `tests/` (organized)
- All files in `debug/` (organized)
- All documentation files (*.md)
- All configuration files (docker-compose.yml, portainer-stack*.yml, etc.)

